# poc/

Archiwum. Dawniej tu były `shoot.py`, `make-override.py`, `serve.sh` — zostały
przeniesione do:

- `domains/capture/` (shoot.py)
- `domains/override/` (make-override.py)
- `cli/serve_cmd.py` (serve.sh)

`poc/media/` pozostaje jako katalog na screenshoty serwowane przez `cli serve`.